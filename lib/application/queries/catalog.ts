/**
 * Public catalog facade with DB-first + static fallback policy.
 * Implementation: `lib/services/public-data`.
 */
export {
  getServices,
  getProjects,
  getTestimonials,
  getHomePageData,
} from "@/lib/services/public-data";
